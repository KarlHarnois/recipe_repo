<%= render 'title', text: 'Recipes' %>

<%= render 'table', columns: ['Name'], rows: @recipes do |recipe| %>
  <%= render 'td' do %>
    <%= link_to recipe.name, recipe_path(recipe) %>
  <% end %>
<% end %>
