<%= render 'title', text: @recipe.name %>

<%= render 'table', columns: %w[Ingredients], rows: @recipe.ingredients do |ingredient| %>
  <% presenter = IngredientPresenter.new(ingredient) %>

  <%= render 'td' do %>
    <%= presenter.name_with_quantity %>
  <% end %>
<% end %>
